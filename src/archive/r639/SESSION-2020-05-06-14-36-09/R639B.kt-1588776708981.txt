fun main() {
    val (n, m) = readLine()!!.split(" ").map { it.toInt() }
    val a = Array(n) { readLine()!!.toCharArray() }
    println(solveMagnet(a))
}

fun solveMagnet(a: Array<CharArray>): Int {
    for (i )
}
